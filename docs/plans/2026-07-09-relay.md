# relay — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer le skill `relay` (transport d'idées cross-harness via une issue Linear dans le projet Handoffs) tel que spécifié dans `docs/specs/2026-07-09-relay-design.md`.

**Architecture:** Un skill markdown pur (`skills/relay/SKILL.md`) qui orchestre 4 verbes (Déposer / Lister / Reprendre / Libérer) via les tools MCP caserne existants — zéro développement serveur. Un fichier de référence `references/pocket-prompt.md` porte le prompt de poche pour les web apps. Tests comportementaux par subagents (baseline RED puis 3 scénarios GREEN).

**Tech Stack:** Markdown (skill), MCP caserne (tools `create_issue` / `list_issues` / `get_issue` / `update_issue` / `comment_issue`, signatures vérifiées dans `erom-agence-control-plane/src/server.ts:52-116` le 2026-07-09), bun (validation frontmatter), tool Agent (tests par subagents).

## Global Constraints

- Branche de travail : `feat/anti-secret-prompt-guard` (branche courante du repo, porte déjà le nettoyage plugin et le fix session-end).
- `docs/` est gitignoré depuis le commit `3fb18e1` (conception interne non distribuée) : ce plan et la spec ne se committent PAS. Seuls `skills/` et les deux manifests se committent.
- Messages user-facing en français ; le contenu d'un relay est du markdown agnostique (aucun jargon propre à un harness).
- Métadonnées Linear fixes et non négociables : `project: "Handoffs"`, `labels: ["handoff"]`, `state: "Todo"` au dépôt, `notify: false`. **Piège vérifié** : sans `state` explicite, `create_issue` crée en `Backlog` (défaut, `src/server.ts:58`) et le relay devient invisible du listing `state: "Todo"`. Noms de statuts valides du registre : Backlog, Todo, Specification, Implementation, Done, Canceled, Duplicate (`~/.config/caserne/employees.yml:20-27`, vérifié 2026-07-09).
- Aucun fichier créé hors `skills/relay/` et les manifests. Pas de tâche README (vérifié : le README ne contient qu'un logo, pas de table de skills).
- Jamais `rm` : `trash` si une suppression s'impose.

---

### Task 1: Le skill `skills/relay/SKILL.md`

**Files:**
- Create: `skills/relay/SKILL.md`

**Interfaces:**
- Consumes: tools MCP caserne existants (aucun changement serveur).
- Produces: le SKILL.md complet que la Task 3 injecte verbatim dans ses prompts de test, et le renvoi `references/pocket-prompt.md` que la Task 2 matérialise.

- [ ] **Step 1: Baseline RED — vérifier l'échec sans le skill**

Dispatcher un subagent (tool Agent, `subagent_type: general-purpose`) avec EXACTEMENT ce prompt (qui ne contient PAS le skill) :

```text
Tu es un agent IA dans un harness de dev, équipé du serveur MCP caserne (tools disponibles : create_issue, list_issues, get_issue, update_issue, comment_issue, whoami, entre autres). Conversation en cours : l'utilisateur discutait d'un bug de chunking. Il dit maintenant : "bon, avant que j'oublie : dépose cette idée pour plus tard, un dashboard temps réel des sessions agents avec leur consommation de tokens, je la creuserai sur Claude Code avec Opus. On en a parlé vite fait : soit un TUI, soit une page web servie par le control-plane, plutôt web pour le partage."

Réponds EXACTEMENT comme tu répondrais dans le harness : ton texte à l'utilisateur, plus tes appels d'outils au format nom({ args en JSON }). N'invente pas le résultat des appels. Ne pose pas de question sur l'environnement : agis avec ce que tu as.
```

Expected FAIL (à documenter en 2 lignes dans le rapport de tâche) : l'agent ne connaît ni le projet `Handoffs`, ni le label `handoff`, ni le gabarit `## Idée` / `## Next` — il improvise (mauvais projet, pas de state Todo, structure libre). C'est la justification RED du skill.

- [ ] **Step 2: Écrire `skills/relay/SKILL.md`**

Contenu complet (verbatim) :

````markdown
---
name: relay
description: "Transport d'une idée en gestation via une issue Linear (projet Handoffs) pour la reprendre sur n'importe quel harness. Déclenche quand l'utilisateur demande explicitement de déposer, lister, reprendre ou libérer un relay : 'passe le relais', 'dépose cette idée', 'relaie ça', 'sauvegarde cette idée pour plus tard', 'mes relays', 'mes idées en attente', 'reprends le relay X', 'reprends l'idée du dashboard', 'libère le relay'. Ne déclenche JAMAIS spontanément quand une idée est simplement évoquée sans demande de dépôt."
user-invocable: true
---

# relay

Un relay = **une issue Linear** dans le projet `Handoffs` qui transporte une **idée en phase gazeuse** : après l'étincelle, avant la pipeline. C'est un passage de témoin curé, pas un dump de conversation. Vise « une session fraîche, sur n'importe quel harness et n'importe quel modèle, reprend sans poser de question bête ».

Quatre verbes : **Déposer**, **Lister**, **Reprendre**, **Libérer**. Intention ambiguë → question fermée, ne devine pas.

## Frontières (ce qu'un relay n'est pas)

- L'idée a un brainstorm formel ou une spec committée ? **Le repo git est le véhicule** : libère le relay en pointant la spec. Un relay ne transporte jamais l'état d'un chantier (git, fichiers, blockers).
- La survie intra-harness (/clear, coupure quota) n'est pas du relay : TaskList et la mémoire native s'en chargent.

## Gabarit

Titre : l'idée en 3-6 mots, sans ponctuation finale.

Description markdown **agnostique** : aucun jargon propre à un harness, aucun chemin sans explication (le relay sera peut-être lu par un autre modèle, ailleurs).

    ## Idée
    (2-4 phrases : quoi, pour qui, pourquoi maintenant)

    ## Next
    (la prochaine étape d'exploration, concrète et petite)

    ## Déclencheur      ← facultatif : d'où ça vient, recharge le contexte mental
    ## Exploré          ← facultatif : pistes discutées ET écartées, avec le pourquoi
    ## Cible            ← facultatif : repo/projet pressenti, harness ou modèle conseillé

Minimum vital : `## Idée` + `## Next`. Les sections facultatives s'ajoutent quand elles portent quelque chose, jamais pour remplir. Un repo cible existe déjà sur le disque → passe son path via le paramètre `workspace` de `create_issue`, pas en prose.

## Déposer

1. Rédige le draft depuis le contexte de la conversation (ne redemande pas ce qui a déjà été dit).
2. Montre le draft complet (titre + description) et demande confirmation.
3. Confirmé :

```
create_issue({
  title: <titre>,
  description: <gabarit rempli>,
  project: "Handoffs",
  labels: ["handoff"],
  state: "Todo",
  notify: false
})
```

(`state: "Todo"` obligatoire : le défaut est Backlog, invisible du listing. `notify: false` : une idée perso ne spamme pas un canal Slack.)

4. Confirme : `Relay déposé : "<titre>" → EAT-XXX` + URL.

## Lister

```
list_issues({ project: "Handoffs", state: "Todo" })
```

« tous » → omettre `state` ; « les finis » → `state: "Done"`. Affichage compact, toujours avec l'identifier (`list_issues` ne renvoie pas de date : n'en invente pas) :

```
=== Relays (3 Todo) ===
1. [EAT-170] Dashboard v3 temps réel
2. [EAT-165] Connecteur OAuth Stalwart
3. [EAT-158] Bug chunking E5
```

Vide : `=== Relays (0 Todo) ===` + `Rien en attente.`

## Reprendre

- Identifier fourni → `get_issue({ id: "EAT-XXX" })`.
- Sinon : `list_issues({ project: "Handoffs", query: <mots du titre>, limit: 5 })` pour résoudre l'identifier (ce résumé n'a pas de description) — match unique → l'utiliser ; plusieurs → montrer la liste (titres seuls) et demander lequel. Une fois l'identifier résolu, `get_issue({ id })` pour charger le payload complet.
- Affiche le payload complet, puis reformule le **Next** en 2 phrases max.
- **Pont pipeline** : si le skill `superpowers:brainstorming` est disponible dans ce harness et que l'intention est d'approfondir l'idée, propose d'enchaîner dessus avec le contenu du relay comme brief.
- Ne libère jamais automatiquement à la reprise. L'utilisateur dit « c'est bon, je reprends » → propose de libérer.

## Libérer

Un véhicule a pris le relais (spec committée, repo, PR) ? Pointe-le d'abord :

```
comment_issue({ id: "EAT-XXX", body: "Repris → <spec/repo/PR>" })
```

Puis :

```
update_issue({ id: "EAT-XXX", state: "Done" })
```

Confirme : `Relay "<titre>" libéré (EAT-XXX → Done).`

## Contraintes

- Ne fixe jamais d'autre champ Linear que title/description/project/labels/state (pas de priority, milestone, cycle, assignee, delegate).
- Ne supprime jamais un relay : uniquement `Todo → Done`, l'historique reste.
- Confirme toujours avant création ; affiche toujours `EAT-XXX`.
- Côté web apps (claude.ai, chatgpt.com), ce skill n'existe pas : le connecteur caserne et le prompt de poche (`references/pocket-prompt.md` dans ce dossier de skill) couvrent le dépôt.
````

- [ ] **Step 3: Valider le frontmatter**

Run (depuis la racine du repo plugin) — script exécuté et validé le 2026-07-09 sur `skills/session-end/SKILL.md` (`OK session-end 423 chars`) :

```bash
bun -e '
const fs = require("fs");
const path = process.argv[1];
const txt = fs.readFileSync(path, "utf8");
const m = txt.match(/^---\n([\s\S]*?)\n---/);
if (!m) throw new Error("frontmatter absent");
const name = m[1].match(/^name:\s*(\S+)\s*$/m)?.[1];
const desc = m[1].match(/^description:\s*"(.+)"\s*$/m)?.[1];
if (!name || !/^[a-z0-9-]+$/.test(name)) throw new Error("name invalide: " + name);
if (!desc || desc.length < 50) throw new Error("description trop courte");
if ((name + desc).length > 1024) throw new Error("frontmatter > 1024 chars");
console.log("OK", name, desc.length + " chars");
' skills/relay/SKILL.md
```

Expected: `OK relay <N> chars` avec N entre 400 et 700. Toute erreur = corriger le frontmatter avant de continuer.

- [ ] **Step 4: Commit**

```bash
git add skills/relay/SKILL.md
git commit -m "feat(skills): relay — transport d'idées cross-harness (v2 du handoff)"
```

---

### Task 2: Le prompt de poche web apps

**Files:**
- Create: `skills/relay/references/pocket-prompt.md`

**Interfaces:**
- Consumes: le renvoi `references/pocket-prompt.md` déjà présent dans le SKILL.md (Task 1).
- Produces: le texte canonique que Romain colle en custom instructions claude.ai / projet ChatGPT.

- [ ] **Step 1: Écrire `skills/relay/references/pocket-prompt.md`**

Contenu complet (verbatim) :

```markdown
# Prompt de poche — déposer un relay depuis les web apps

À coller dans les custom instructions (claude.ai) ou les instructions d'un projet ChatGPT, sur un compte où le connecteur caserne (MCP Streamable HTTP) est branché :

> Quand je te demande de déposer/relayer une idée : crée une issue Linear via le connecteur caserne avec `create_issue({ project: "Handoffs", labels: ["handoff"], state: "Todo", notify: false })`. Titre 3-6 mots. Description markdown : `## Idée` (2-4 phrases) et `## Next` (prochaine étape concrète) obligatoires ; `## Déclencheur`, `## Exploré`, `## Cible` seulement s'ils portent quelque chose. Montre-moi le draft avant de créer.

La reprise se fait de préférence sur un harness équipé du skill `relay` (« reprends le relay X »). Sans connecteur, le fallback reste l'app Linear (mobile/web) : le réceptacle est une simple issue du projet Handoffs.
```

Note : `state: "Todo"` figure dans le prompt de poche pour la même raison que dans le skill (défaut Backlog = relay invisible du listing). La spec l'omettait dans son exemple ; le plan corrige, la spec fait foi sur tout le reste.

- [ ] **Step 2: Vérifier le renvoi depuis le skill**

Run: `command grep -c 'references/pocket-prompt.md' skills/relay/SKILL.md`
Expected: `1`

- [ ] **Step 3: Commit**

```bash
git add skills/relay/references/pocket-prompt.md
git commit -m "feat(skills): relay — prompt de poche web apps (connecteur caserne)"
```

---

### Task 3: Tests comportementaux GREEN (3 scénarios subagent)

**Files:**
- Modify: `skills/relay/SKILL.md` (seulement si un scénario échoue)

**Interfaces:**
- Consumes: le SKILL.md complet de la Task 1, injecté verbatim dans chaque prompt à l'emplacement `<<<SKILL>>>`.
- Produces: un skill dont les 3 comportements critiques sont vérifiés (dépôt conforme, non-déclenchement spontané, reprise avec pont pipeline).

Méthode : chaque scénario est un dispatch Agent (`subagent_type: general-purpose`). Ces tests vérifient l'INTENTION d'appel (format textuel), pas l'intégration MCP réelle — celle-ci relève des critères de succès de la spec, à l'usage. Lire chaque sortie en entier (pas de scoring au grep).

- [ ] **Step 1: Scénario S1 — dépôt conforme**

Prompt exact (remplacer `<<<SKILL>>>` par le contenu intégral de `skills/relay/SKILL.md`) :

```text
Tu es un agent IA dans un harness de dev, équipé du serveur MCP caserne (tools disponibles : create_issue, list_issues, get_issue, update_issue, comment_issue, whoami, entre autres). Tu disposes du skill suivant :

<<<SKILL>>>

Conversation en cours : l'utilisateur discutait d'un bug de chunking. Il dit maintenant : "bon, avant que j'oublie : dépose cette idée pour plus tard, un dashboard temps réel des sessions agents avec leur consommation de tokens, je la creuserai sur Claude Code avec Opus. On en a parlé vite fait : soit un TUI, soit une page web servie par le control-plane, plutôt web pour le partage."

Réponds EXACTEMENT comme tu répondrais dans le harness : ton texte à l'utilisateur, plus tes appels d'outils au format nom({ args en JSON }). N'invente pas le résultat des appels. Ne pose pas de question sur l'environnement : agis avec ce que tu as.
```

PASS si TOUT est vrai : (a) draft complet montré (titre + description) AVANT tout appel ; (b) demande de confirmation explicite ; (c) l'appel prévu est `create_issue` avec `project: "Handoffs"`, `labels: ["handoff"]`, `state: "Todo"`, `notify: false` ; (d) description avec `## Idée` (2-4 phrases) et `## Next` concret ; (e) `## Exploré` présent (l'utilisateur a donné TUI vs web + le choix web) ; (f) titre 3-6 mots ; (g) aucun champ interdit (priority, assignee, milestone, cycle, delegate).

- [ ] **Step 2: Scénario S2 — non-déclenchement spontané**

Prompt exact (même préambule harness + `<<<SKILL>>>`) puis :

```text
L'utilisateur, en plein debug d'un test flaky, dit : "marrant, ce matin sous la douche j'ai pensé à un système de replay des sessions, enfin bref. Donc ce test, il pète seulement en CI ?"

Réponds EXACTEMENT comme tu répondrais dans le harness.
```

PASS si : la réponse traite le test flaky ; AUCUN `create_issue` ; aucune insistance (une demi-phrase du type « dis-moi si tu veux que je dépose l'idée du replay en relay » est tolérée, pas obligatoire). FAIL si dépôt, draft non sollicité, ou question bloquante sur l'idée.

- [ ] **Step 3: Scénario S3 — reprise (recherche + chaînage) + pont pipeline**

Prompt exact (même préambule harness, en ajoutant à la liste des capacités : « le skill superpowers:brainstorming est disponible dans ce harness ») + `<<<SKILL>>>` puis :

```text
L'utilisateur dit : "reprends le relay du dashboard" (pas d'identifier donné). Voici ce qui s'est déjà passé dans cette conversation — les deux appels ont eu lieu, tu déroules la suite :

Appel 1 : list_issues({ project: "Handoffs", query: "dashboard", limit: 5 })
Résultat 1 (simulé, forme réelle du tool — un résumé, pas de description) :
[{ "identifier": "EAT-171", "id": "8f2c1e40-...", "title": "Dashboard temps réel sessions", "url": "https://linear.app/erom/issue/EAT-171", "state": "Todo", "delegateId": null }]

Match unique → tu as enchaîné avec get_issue({ id: "EAT-171" }).
Résultat 2 (simulé) :
{ "identifier": "EAT-171", "title": "Dashboard temps réel sessions", "url": "https://linear.app/erom/issue/EAT-171", "state": "Todo", "description": "## Idée\nUn dashboard temps réel des sessions agents avec leur consommation de tokens.\n\n## Next\nBrainstormer le choix TUI vs page web servie par le control-plane.\n\n## Exploré\n- TUI : léger mais pas partageable\n- Web : partage facile, plutôt retenu" }

Déroule EXACTEMENT la suite : ton texte à l'utilisateur et tes éventuels appels d'outils.
```

PASS si : payload restitué (au moins Idée + Next) ; **Next** reformulé en 2 phrases max ; pont proposé vers `superpowers:brainstorming` avec le contenu du relay comme brief ; AUCUN `update_issue` (pas de libération automatique) ; aucune donnée inventée au-delà des deux résultats fournis (pas de date fabriquée, pas de champ Linear halluciné). FAIL si l'issue est fermée, si le Next est ignoré, ou si le pont n'est pas proposé.

- [ ] **Step 4: Itérer si échec**

Pour chaque scénario FAIL : identifier la zone du SKILL.md en cause (S2 → description/garde ; S1 → sections Gabarit/Déposer ; S3 → section Reprendre), appliquer la correction minimale, re-dispatcher UNIQUEMENT le scénario échoué. Répéter jusqu'à 3 PASS. Si un scénario échoue encore après 2 corrections : STOP, rapporter les sorties verbatim à Romain (signal possible pour le plan B de la spec).

- [ ] **Step 5: Commit (seulement si le SKILL.md a été modifié)**

```bash
git add skills/relay/SKILL.md
git commit -m "revise(relay): durcissement post-tests comportementaux"
```

---

### Task 4: Version 2.8.0

**Files:**
- Modify: `plugin.json:4` (`"version": "2.7.2"`)
- Modify: `.claude-plugin/plugin.json:5` (`"version": "2.7.2"`)

**Interfaces:**
- Consumes: rien.
- Produces: release 2.8.0 (nouvelle capability = bump minor ; convention proposée, l'historique de bump a été perdu dans le rename du matin).

- [ ] **Step 1: Bumper les deux manifests**

Dans `plugin.json` ET `.claude-plugin/plugin.json`, remplacer :

```json
  "version": "2.7.2",
```

par :

```json
  "version": "2.8.0",
```

- [ ] **Step 2: Vérifier la synchro**

Run: `command grep -h '"version"' plugin.json .claude-plugin/plugin.json`
Expected: deux lignes identiques `"version": "2.8.0",`

- [ ] **Step 3: Commit**

```bash
git add plugin.json .claude-plugin/plugin.json
git commit -m "chore(release): 2.8.0 — skill relay"
```

---

## Hors périmètre de ce plan (rappel spec)

- Routine launchd pour `caserne serve` (prérequis opérationnel web apps).
- Vérification du toggle Developer mode ChatGPT (action manuelle Romain, 10 s).
- Refresh tokens (`offline_access`) et `readOnlyHint` côté control-plane (dev éventuel, autre repo).
- Migration du skill `inbox` vers le tool renommé `project_status` (dette signalée, hors relay).
- Plan B (`drop_idea` / `pick_idea` serveur) : uniquement si le critère de succès 3 de la spec échoue à l'usage.
